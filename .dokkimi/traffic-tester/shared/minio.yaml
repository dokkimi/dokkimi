type: SERVICE
name: minio
image: minio/minio:latest
port: 9000
healthCheck: /minio/health/live
command: ['server', '/data']
env:
  - name: MINIO_ROOT_USER
    value: minioadmin
  - name: MINIO_ROOT_PASSWORD
    value: minioadmin
